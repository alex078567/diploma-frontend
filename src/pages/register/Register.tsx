import { RegisterSection } from 'src/widgets/form-sections';

const Register = () => {
	return (
		<main className="form-main register-main">
			<RegisterSection />
		</main>
	);
};

export default Register;
